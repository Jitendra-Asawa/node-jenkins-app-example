node{
	stage('Hello'){
		sh "echo 'Hello'"
	}
	stage('Build'){
		sh "echo 'Master'"
	}
	// stage('Branch'){
		// sh "git branch deploy"
		// sh "git checkout deploy"
		// sh "git tag -a v1.7"
		// sh "git commit -m 'Added Tag v1.7'"
		// sh "git checkout master"
	// }
}
